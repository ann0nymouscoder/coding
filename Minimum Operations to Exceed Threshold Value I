class Solution {
public:
    int minOperations(vector<int>& nums, int p) 
    {
        int count=0;
        int k=nums.size();
        for(int i=0;i<k;i++)
        {
            if(nums[i]<p)
            {
                count++;
            }
        }
        return count;
    }
};
